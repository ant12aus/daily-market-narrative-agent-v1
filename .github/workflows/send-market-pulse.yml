name: send-market-pulse

on:
  schedule:
    # Fire at both UTC times that can correspond to 08:15 ET across DST.
    - cron: '15 12 * * 1-5'
    - cron: '15 13 * * 1-5'
  workflow_dispatch:
    inputs:
      force_send:
        description: 'Send immediately (ignore 08:15 ET gate)'
        required: false
        default: 'false'

jobs:
  send:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Build dynamic report
        run: python build_report.py

      - name: Gate + Send
        env:
          # SMTP
          SMTP_SERVER: smtp.gmail.com
          SMTP_PORT: 587
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}
          FROM_EMAIL: ${{ secrets.FROM_EMAIL }}
          TO_EMAIL: ${{ secrets.TO_EMAIL }}
          REPLY_TO: ${{ secrets.REPLY_TO }}

          # Content paths
          HTML_PATH: 'morning_market_pulse.html'     # built in previous step
          # NARRATIVE_PATH: 'narrative.json'         # (optional) only change if you move it

          # Optional toggles / add-ons
          # CC_EMAIL: ''
          # BCC_EMAIL: ''
          # DRY_RUN: 'false'

          # Manual override
          FORCE_SEND: ${{ github.event.inputs.force_send }}
        run: |
          echo "Trigger: ${GITHUB_EVENT_NAME}"
          echo "FORCE_SEND: ${FORCE_SEND:-false}"
          echo "NY time: $(TZ=America/New_York date)"
          ls -l "${HTML_PATH}" || true

          # Enforce the 08:15 ET gate:
          # - Always enforce for scheduled runs
          # - For manual runs, enforce unless FORCE_SEND=true
          ENFORCE_GATE="false"
          if [ "${GITHUB_EVENT_NAME}" = "schedule" ]; then
            ENFORCE_GATE="true"
          elif [ "${GITHUB_EVENT_NAME}" = "workflow_dispatch" ] && [ "${FORCE_SEND}" != "true" ]; then
            ENFORCE_GATE="true"
          fi

          if [ "${ENFORCE_GATE}" = "true" ]; then
            if [ "$(TZ=America/New_York date +%H%M)" != "0815" ]; then
              echo "Not 08:15 America/New_York; skipping."
              exit 0
            fi
          fi

          echo "Proceeding — sending email now..."
          python send_mail.py
